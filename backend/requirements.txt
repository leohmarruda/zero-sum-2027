# Kept in sync with [project.dependencies] in pyproject.toml for local pip.
# Vercel installs from pyproject.toml (preferred when both exist).
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20.0
alembic>=1.14.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
httpx>=0.28.0
litellm>=1.50.0
tenacity>=8.0.0
